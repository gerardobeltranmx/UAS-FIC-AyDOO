#include <iostream>
#include <iomanip>
using namespace std;

int main() {
  
    float cantidad, suma, promedio;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

    cin >> cantidad;
    suma = suma + cantidad;

  
    cin >> cantidad;
    suma = suma + cantidad;

    promedio = suma /12;

    cout <<"$"<< fixed << setprecision(2) << promedio << endl;
  
   return 0;
}