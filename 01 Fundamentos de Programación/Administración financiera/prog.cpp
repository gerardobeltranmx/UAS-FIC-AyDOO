#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double cantidad, suma=0, promedio;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;
    cin >> cantidad;
    suma = suma + cantidad;

    promedio = suma /12;

    cout <<"$" << fixed << setprecision(2) << promedio;
  
   return 0;
}